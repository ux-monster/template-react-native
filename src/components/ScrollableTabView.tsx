import React from 'react';
import {View} from 'react-native';

type Props = {};

const ScrollableTabView = (props: Props) => {
  return <View>ScrollableTabView</View>;
};

export default ScrollableTabView;

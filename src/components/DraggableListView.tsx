import React from 'react';
import {View} from 'react-native';

type Props = {};

const DraggableListView = (props: Props) => {
  return <View>DraggableListView</View>;
};

export default DraggableListView;

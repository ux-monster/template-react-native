import React from 'react';
import {View} from 'react-native';

type Props = {};

const KeyboardAvoidingScreen = (props: Props) => {
  return <View>KeyboardAvoidingScreen</View>;
};

export default KeyboardAvoidingScreen;
